apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRole
metadata:
  name: {{ .Chart.Name }}
rules: 
  - apiGRoups: {{ .Values.clusterRole.apigroups }}
    ressources: {{ .Values.clusterRole.ressources }}
    verbs: {{ .Values.clusterRole.verbs }}
